spring:
  datasource:
    url: jdbc:mysql://localhost:3306/imageApi?useSSL=false&serverTimezone=UTC
    username: ginthar
    password: C0ding@666
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    org:
      hibernate:
        type: trace
