server:
  port: 8080

spring:
  main:
    allow-circular-references: true
  datasource:
    url: jdbc:mysql://localhost:3306/opendive
    username: ginthar
    password: C0ding@666
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      current_session_context_class: org.springframework.orm.hibernate5.SpringSessionContext
    show-sql: true

